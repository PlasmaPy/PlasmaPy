Development dependencies are now defined using dependency groups rather than via optional
dependencies because these dependencies are not intended for use by end users. Commands
like ``pip install plasmapy[docs,tests]`` will no longer work.
